# requirements.txt — Dépendances du backend Smartovate Chatbot
# Installer avec : pip install -r requirements.txt

# Framework web
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Gestion des variables d'environnement
pydantic-settings==2.2.1

# Azure OpenAI SDK (supporte Azure nativement depuis v1.x)
openai==1.30.1

# Bot Framework SDK Python (intégration FastAPI native — sans aiohttp)
botbuilder-core==4.14.8
botframework-connector==4.14.8

# Azure AI Search SDK
azure-search-documents==11.6.0b4
azure-core==1.30.1

# Ingestion documentaire (RAG — US 1.2)
pdfplumber==0.11.0        # Extraction texte depuis les PDF
python-docx==1.1.2        # Extraction texte depuis les fichiers DOCX
tiktoken==0.7.0           # Comptage précis des tokens pour le chunking

# Utilitaires
python-dotenv==1.0.1
httpx==0.27.0
